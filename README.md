# HAP's silly 404

[![Netlify Status](https://api.netlify.com/api/v1/badges/e1c80bb8-f849-4c85-9a20-c10b0f9280fb/deploy-status)](https://hap-silly-404.netlify.app)

A custom 404 page that responds to lost visitors with a passive-aggressive roast from HAP — the Hypertext Anthropomorphic Person. Built as a teaching project demonstrating serverless functions, browser security, and graceful degradation.

> **See it live →** [hap-silly-404.netlify.app/404](https://hap-silly-404.netlify.app/404)

![Screenshot of the HAP 404 page](images/hap-silly-404.netlify.app_kflflf.png)

---

## What this is

When someone lands on a page that doesn't exist, HAP appears in one of 20 illustrated poses and delivers a dry, one-line roast generated by the Groq API (`llama-3.3-70b-versatile`). If the API is unavailable, the page falls back to eight hardcoded insults — so the roast always loads.

The project is intentionally simple: no build step, no framework. Vanilla HTML, CSS, and JavaScript on the front end; a single ES module serverless function on the back end. Dev tooling (ESLint, Prettier) is available via `npm install` but nothing needs to be compiled or bundled.

---

## How it works

1. A visitor hits any URL that doesn't match a real file
2. Netlify's redirect rule (`/*` → `/404.html`, status 404) serves the 404 page
3. `js/404.mjs` runs on page load — it picks a random HAP pose from Cloudinary and fetches `/.netlify/functions/insult`
4. `insult.mjs` checks the request origin, calls the Groq API with a rotating prompt angle, and returns `{ insult, source }` JSON
5. The roast appears via `textContent` (never `innerHTML`) — no XSS risk from the API response

The "Generate a new roast" button repeats steps 3–5 without a page reload.

---

## Tech stack

- **Vanilla HTML/CSS/JS** — no build step, no framework; `package.json` is dev tooling only
- **Netlify Functions v2** — ES module serverless function using Web API `Request`/`Response`
- **Groq API** — `llama-3.3-70b-versatile`, free tier, `max_tokens: 60`
- **Cloudinary** — hosts 20 HAP pose illustrations, served with `f_auto,q_auto`

---

## Security features demonstrated

This project is hardened beyond the minimum — the `docs/` folder explains how and why each decision was made.

- **Content Security Policy** — external CSS/JS only, Cloudinary in `img-src`, currently in Report-Only mode (observe before enforcing)
- **CORS** — function allows only its own origin; defaults to `http://localhost:8888` in dev; fails closed (500) in production if `SITE_URL` is missing
- **Origin/Referer check** — server-side guard that rejects non-browser callers before spending an API call
- **Rate limiting** — 4 requests per IP+domain per 60-second window via Netlify Functions config
- **Security headers** — `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, HSTS (2-year with preload), `Permissions-Policy`
- **Safe rendering** — roast text set via `textContent`; response shape validated with optional chaining before use

---

## Beginner's guide

### What you need

- A [Netlify account](https://netlify.com) (free)
- A [Groq API key](https://console.groq.com) (free tier)
- The [Netlify CLI](https://docs.netlify.com/cli/get-started/) installed: `npm install -g netlify-cli`

### Setup

```bash
git clone https://github.com/YOUR_USERNAME/hap-silly-404.git
cd hap-silly-404
npm install
netlify login
netlify link
```

Set your environment variables in the Netlify dashboard under **Site configuration → Environment variables**:

- `GROQ_API_KEY` — required for live roasts; without it the function returns hardcoded fallback roasts
- `SITE_URL` — required in production only; set to your site's canonical origin (e.g. `https://hap-silly-404.netlify.app`); the function fails closed if this is missing in production
- `GROQ_MODEL` — optional; defaults to `llama-3.3-70b-versatile`

### Running locally

```bash
netlify dev
```

Visit `http://localhost:8888/this-page-does-not-exist` to trigger the 404 page. The function auto-detects `NETLIFY_DEV=true` and allows `http://localhost:8888` as the origin — no local env config needed beyond linking the site.

> Always use port **8888**, not 3999. The CLI runs the static server on 3999 internally; 8888 is where the function runtime and redirects live.

Any path that doesn't match a real file will trigger the 404:

```
http://localhost:8888/this-page-does-not-exist
http://localhost:8888/oops
http://localhost:8888/404
```

---

## Tutorials

The `docs/` folder has four student-facing tutorials. They build on each other — reading order matters.

1. [CORS is hard and everyone hates it](docs/cors-is-hard-and-everyone-hates-it.md) — why CORS exists, how the browser enforces it, the five errors you'll actually see, and a hands-on exercise using this repo
2. [Local debugging with DevTools and netlify dev](docs/local-debugging-with-devtools-and-netlify-dev.md) — setup, the two-window dev workflow, reading function logs, and walkthroughs of six real failure modes
3. [Hardening walkthrough — applying each audit fix](docs/hardening-walkthrough-applying-each-audit-fix.md) — read-along explaining every security change made to this app: CSP, fail-closed CORS, rate limiting, origin checks, and defensive response parsing
4. [Secretlint — keeping secrets out of git](docs/secretlint-keeping-secrets-out-of-git.md) — why leaked API keys are dangerous, how secretlint blocks accidental commits, and what to do when it fires

---

## Project structure

```
├── index.html                   # Landing page
├── 404.html                     # The 404 page
├── css/
│   ├── style.css                # Shared styles (both pages)
│   └── 404.css                  # 404-page-specific styles
├── js/
│   └── 404.mjs                  # Client JS: pose picker, fetch, DOM updates
├── netlify/
│   └── functions/
│       └── insult.mjs           # Serverless function: Groq call, CORS, rate limit
├── docs/                        # Student-facing tutorials (ships with repo)
├── netlify.toml                 # Redirects, headers, functions config
├── eslint.config.js             # ESLint 9 flat config
├── .prettierrc                  # Prettier config
├── .secretlintrc.cjs            # Secretlint config (CJS required by config loader)
├── .husky/                      # Git hooks (pre-commit runs lint-staged)
├── package.json                 # Dev tooling only (lint, format, check scripts)
└── .env.example                 # Template for local env vars
```

---

## Contributing

Fork the repo and open a pull request. A few things to know:

- Run `npm run check` before opening a PR — runs lint, format check, and secretlint in one shot
- The pre-commit hook (husky + lint-staged) runs these automatically on staged files at `git commit`
- All colors use `hsl()` via CSS custom properties prefixed `--hap-*` — don't add hex or rgb values
- DOM updates use `textContent`, not `innerHTML`
- To add a HAP pose: upload the image to Cloudinary under `canvas/hap/`, then add an entry to the `HAP_POSES` array in `js/404.mjs` with `id`, `alt`, and `caption`
- The serverless function only accepts GET requests and requires an `Origin` or `Referer` header matching `ALLOWED_ORIGIN` — keep that check in place

---

## License

MIT License — Cynthia Teeters

---

_HAP™ is a trademark of Cynthia Teeters. All HAP illustrations are © Cynthia Teeters, all rights reserved. The MIT license covers the code in this repository only and does not extend to the HAP character, name, or illustrations._
